spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/cursovaia
    username: cursach
    password: 12345
    driver-class-name: org.postgresql.Driver
  freemarker:
    expose-request-attributes: true

logging:
  level:
    org:
      springframework:
        security: DEBUG
